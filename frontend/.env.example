# Razorpay publishable key. Leave blank and the app tells patients that online
# payment is not set up rather than opening a checkout that cannot work.
# The key secret is never here: it belongs to the backend, which uses it to
# verify the webhook that actually confirms an appointment.
VITE_RAZORPAY_KEY_ID=
